1) "html" contains the webpages to interface with eval server
2) "server" contains the main code for the Eval server

3) the order of operations are
	a) run the Eval server
	b) open the web interface i.e index.html. Submit the required information
	c) Connect your eval client to eval server from Ultra96 via "TCP/IP". 
	   The TCP port on which to connect, will be displayed on the webpage.